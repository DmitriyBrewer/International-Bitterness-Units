import React, { useState } from "react";

function FormWort(props){
    const [value, inputValue] = useState("");
    const [value1, inputValue1] = useState("");
    const [value2, inputValue2] = useState("");
    const [butPlato, newClassPlato] = useState('butPurp');
    const [butOG, newClassOG] = useState('but');
    const [cl, newCl] = useState('inPut inPut-design');
    const [cl1, newCl1] = useState('inPut PlatoSG inPut-design');
    const [cl2, newCl2] = useState('inPut inPut-design');
    const [cl4, newCl4] = useState('');



    function SubmitPlato(e) {
        e.preventDefault();
        // e.className = "butPurp"
        newClassPlato('butPurp');
        newClassOG('but');
        var Plato = 'Plato'
        return console.log(Plato);
    };

    function SubmitOG(e) {
        e.preventDefault();
        newClassPlato('but');
        newClassOG('butPurp');
        var SG = 'SG'
        return console.log(SG);
    };





    function handleChange(e) {
        inputValue(e.target.value);
    };

    function handleChange1(e) {
        inputValue1(e.target.value);
    };

    function handleChange2(e) {
        inputValue2(e.target.value);
    };












    function rightValue (val) {
        if (!val.trim()){
            newCl('inPut inPut-design btn__danger');
            console.log('Invalid value 1');
            return false
          } else {
            newCl('inPut inPut-design agree');
            return true
            };
      };


      function rightValue1 (val) {
        if (!val.trim()){
            newCl1('inPut inPut-design PlatoSG btn__danger');
            console.log('Invalid value 2');
            return false
        } else {
            newCl1('inPut inPut-design PlatoSG agree')
            return true
        };
    };


    function rightValue2 (val) {
        if (!val.trim()){
            newCl2('inPut inPut-design btn__danger');
            console.log('Invalid value 3');
            return false
            } else {
            newCl2('inPut inPut-design agree')
            return true
        }
        };
   








    function handleSubmit(e) {
        
    e.preventDefault();
    
    rightValue(value);
    rightValue1(value1);
    rightValue2(value2);
    
    if(rightValue(value) === false){
            return e
        }
    if(rightValue1(value1) === false){
        return e
    }
    if(rightValue2(value2) === false){
        return e
    }
 

    newCl4("visuall-hidden")
    props.setHidden("visuall-yes");
    props.addValue(value,value1,value2);
      inputValue("");
      inputValue1("");
      inputValue2("");
    }

    const volume = value;//выведем введённое значение в консоль
    const plato = value1;//выведем введённое значение в консоль
    const boiltime = value2;//выведем введённое значение в консоль

    console.log("volume= " + volume);
    console.log("plato= " + plato);
    console.log("boiltime= " + boiltime);


    
    return (
            <form onSubmit={handleSubmit} className={cl4}>
            <li className="inputstyle"><h1 className="h1styleBlack">Введите параметры сусла</h1></li>
            <li className="inputstyle"><input placeholder="Введите объем сусла, л" className={cl} value={value} onChange={handleChange}/></li>
            <li className="inputstyle"><input placeholder="Введите плотность сусла" className={cl1} value={value1} onChange={handleChange1}/>
                <button className={butPlato} onClick={SubmitPlato} type="radio">Plato</button>
                <button className={butOG} onClick={SubmitOG} type="radio">SG</button>
            </li>
            <li className={"inputstyle"}><input id="inputboiltime" placeholder="Введите время кипячения, мин" className={cl2} value={value2} onChange={handleChange2} /></li>
            <li className="inputstyle" >
                <button className="inPut button1" id="buttonNEXT" type="submit">Далее</button>    
            </li>
            </form>
            );
}

export default FormWort;//Wort это сусло